#include <boost/algorithm/string.hpp>
#include <iostream>
#include <vector>
#include <string>

using namespace std;
using namespace boost::algorithm;

void a()
{
    vector<string> k(10,"");
    //cout << boost::algorithm::join(k, " && ") << endl;
    //cout << join(k, " && ") << endl;
}
